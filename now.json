{
    "version": 2,
    "name": "carbon-tracker",
    "builds": [{ "src": "carbon-tracker/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "carbon-tracker/$1" }
    ]
}
